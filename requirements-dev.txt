# Dev-only dependencies on top of the runtime set.
-r requirements.txt

# Test tooling
pytest>=7.0

# Build-script dep (parses ANS OWL dump — used only by scripts/build_cim10_enrichment.py)
rdflib>=7.0
